import fs from 'node:fs';
const config=JSON.parse(fs.readFileSync('src/game-config.json','utf8'));
const descriptions={};
function group(name,text){const keys=Object.keys(config[name]),labels=text.split('|');if(keys.length!==labels.length)throw Error(name+': '+keys.length+' keys / '+labels.length+' labels');keys.forEach((key,i)=>descriptions[name+'.'+key]=labels[i]);}
group('core','求生者奔跑速度，像素/秒|求生者蹲行速度，像素/秒|屠夫基础移动速度，像素/秒|恐惧心跳范围，像素|单人修好一台发电机的基础秒数|修机校准失败后的禁止维修秒数|一名队友完成一个治疗阶段的基础秒数|出口门读条秒数|每次挂钩阶段的秒数|救下挂钩队友的读条秒数|真人被搬运且不主动挣扎时，自动挣脱所需秒数|AI 被搬运时自动挣脱所需秒数|主动挣扎两次有效输入的最短间隔，秒|每次有效挣扎增加的进度，0～1|倒地累计失血死亡时间，秒；被扶起不重置已消耗预算|终局倒计时，秒|屠夫普通攻击距离，像素|屠夫突刺判定距离，像素|攻击扇形半角，弧度|按住攻击达到满蓄力的秒数|藏入衣柜后第一次乌鸦警报延迟，秒|衣柜后续乌鸦警报间隔，秒|屠夫近距离全向感知半径，像素|求生者近距离全向感知半径，像素|屠夫角色可见距离，null 表示无限；不是镜头缩放|求生者角色可见距离，null 表示无限|求生者视野半角，弧度；π 为全向|屠夫视野半角，弧度；π 为全向');
group('movement','求生者不奔跑时的步行速度，像素/秒|倒地爬行速度，像素/秒|屠夫搬运时的移动速度，像素/秒|蓄力时速度乘数|受伤或保护加速时的速度乘数|攻击冷却时屠夫速度乘数|角色之间阻挡的最小中心距离，像素|角色移动的碰撞半径，像素|寻路与部分翻越检查的预留半径，像素|受击与挣脱等加速持续秒数|判定刚刚移动过的时间窗口，秒|快翻需要的实际接近速度，像素/秒|放下被搬运者时的候选位置偏移，像素');
group('actions','命中攻击基础恢复秒数|未命中攻击基础恢复秒数|满蓄力额外恢复秒数，按蓄力比例缩放|超过此蓄力比例触发突刺，0～1|挂人读条秒数|踩碎木板读条秒数|踢发电机读条秒数|踢机直接损失的进度，0～1|每多一名修机者的单人效率折损系数|无人维修时回退速率相对基础修机速率的倍率|倒地自我恢复上限，0～1|自我恢复从 0 到 1 的理论秒数，实际受上限限制|救援后被救者的保护秒数|脱钩动作过渡秒数|抱起以及挂人后的屠夫冷却秒数|主动放下幸存者后的屠夫冷却秒数|达到此挂钩次数时立即献祭|第一阶段主动自救成功概率，0～1|每次主动自救消耗的挂钩时间，秒|主动自救尝试间隔，秒|挣脱搬运后的保护秒数|挣脱搬运对屠夫造成的眩晕秒数|放板砸中屠夫的眩晕秒数|放板命中屠夫的判定半径，像素|踩板过程产生冲击声音的进度点数组，每项 0～1|踢机过程产生踢击声音的进度点，0～1|每名治疗者贡献的治疗倍率，多人相加');
group('traversal','楼梯交互中心距离，像素|上下楼动作秒数|上下楼后的冷却秒数|高处跳落交互距离，像素|跳落动作秒数|屠夫落地后冷却秒数|求生者落地后冷却秒数|放板与翻板交互距离，像素|翻板后距木板中心的偏移，像素|跑动放板跨到另一侧的角色过渡秒数；不是板子倾倒动画时长|快翻板秒数|慢翻板秒数|翻窗交互距离，像素|翻窗落点离窗中心的横向偏移，像素|屠夫翻窗秒数|求生者快翻窗秒数|求生者慢翻窗秒数|放板后的操作冷却秒数|放板后若角色重叠，按顺序尝试的脱困偏移数组，像素');
group('interactions','未单独指定的交互范围，像素|修机交互范围，像素|治疗交互范围，像素|救援交互范围，像素|衣柜交互范围，像素|出口门交互范围，像素|已开始的工作允许保持的最大距离，像素|治疗者靠近患者后的站位距离，像素|救援者靠近挂钩者后的站位距离，像素|其他工作靠近目标后的站位距离，像素|工作时自动靠近目标的指数速度系数，越大靠近越快|抱起倒地幸存者的距离，像素|抓住正在翻窗的受伤幸存者的距离，像素|翻窗抓取方向判定的余弦阈值，越大角度越窄|工作动画判为仍在进行的宽限秒数|保留当前修机交互的宽限秒数|多人治疗/救援工作活跃判定宽限秒数|开启地窖的交互距离，像素；倒地也可立即逃生');
group('lockers','屠夫搜索衣柜秒数|幸存者进出衣柜读条秒数|幸存者进出衣柜后的冷却秒数|从衣柜抓出幸存者的过渡秒数|抓出幸存者后的屠夫冷却秒数|搜空衣柜后的屠夫冷却秒数|柜门交互点相对柜子中心的向下偏移，像素|衣柜碰撞宽度的一半，像素|衣柜碰撞高度的一半，像素');
group('skill','第一次校准的基础等待秒数|校准出现前警告的提前秒数|成功区域起点的最小进度，0～1|成功区域起点额外随机范围，0～1|校准成功区域宽度，0～1|两次校准的最短间隔，秒|校准间隔的额外随机秒数|挣扎校准失败增加的挂钩时间，秒|修机校准失败损失进度，0～1|治疗校准失败损失共享进度，0～1|校准失败后的角色冷却秒数|工作取消后重设的校准等待秒数|校准指针走完整条的秒数');
group('alerts','发电机完成或校准失败位置警报持续秒数|救援发生位置警报持续秒数|移动打断治疗位置警报持续秒数|衣柜乌鸦位置警报持续秒数|踢机/踩板方向提示持续秒数|AI 可调查的声音痕迹持续秒数|奔跑划痕保留秒数|受伤血迹保留秒数|产生划痕/血迹的最短间隔，秒|两次嘲讽触发的最短间隔，秒');
group('match','每局从地面候选点抽取的发电机数量|每局从二楼候选点抽取的发电机数量|修好多少台发电机给出口通电|每局抽取的地上钩子数量，地下室四钩另计|出口开启动画秒数，动画结束后可通过|出口终点到门中心的横向距离，像素|进入出口终点的逃脱判定半径，像素|成功逃脱后到结束对局的过渡秒数|死亡灵魂动画秒数|判断追逐的距离，像素|最近发动攻击也能维持追逐的时间窗口，秒|追逐目标离开后保留状态的秒数|不同楼层目标在普通距离计算中增加的代价，像素|不同楼层在恐惧/音效距离计算中的垂直距离，像素|选择追逐目标时朝向偏差的权重，越大越偏好正前方|保留原追逐对象的评分宽限，越大越不易切换');
group('lunge','突刺开始移动前的起手秒数|突刺开始允许命中判定的秒数|突刺移动及伤害判定结束时刻，距攻击开始的秒数|突刺过程中屠夫额外移动的距离，像素|突刺画面回到待机的总秒数');
group('camera','求生者镜头参数|屠夫镜头参数|手机屠夫额外缩放倍率，越小看得越广|不同屏幕的适配参数');
group('audio','初次进入的默认音量，0～1；已有本地设置优先|嘲讽传播范围，像素|七个嘲讽音频的时长数组，秒，按 TAUNTS 顺序|心跳音频曲线参数|追逐音乐音量相对总音量倍率|维修/治疗工作循环音量倍率|奔跑脚步最短间隔，秒|走路脚步最短间隔，秒|奔跑脚步音量倍率|走路脚步音量倍率');
group('stairsVisual','楼梯画面宽度，像素|楼梯画面长度，像素|楼梯台阶数，整数且至少 3|台阶立面的画面高度，像素|地下室血迹颜色，十进制 RGB|上行楼梯台阶颜色，十进制 RGB|下行楼梯台阶颜色，十进制 RGB|地下室楼梯台阶颜色，十进制 RGB');
group('layout','随机板区生成尝试上限|每局随机板区数，不含固定板子|随机板区纵向布置的概率，0～1|随机候选网格起点|随机候选网格列数|随机候选网格行数|候选网格间距，像素|长板区左侧地形的最短长度，像素|长板区左侧额外随机长度，像素|短板区左侧地形最短长度，像素|短板区左侧额外随机长度，像素|长板区右侧地形长度，像素|短板区右侧地形长度，像素|木板缺口宽度的一半，像素|板区地形厚度，像素|随机板区与其他地形的最小间隔，像素|随机板区与物件/出生点的预留距离，像素|木板前后预留落脚点偏移，像素|每 N 个随机板区设置一个高墙板区，其余低墙|随机板区允许的外边界|禁止生成板区的矩形及额外留白数组|随机板区距石头外框的最小通行留白，像素|空间拥挤时重新布置随机板区的最多次数，保留板区数最多的方案|每 N 个随机板区中第 2 个用成对石头夹板，当前每局 3 组|夹板石头的基础厚度，像素|夹板石头厚度的额外随机范围，像素');
group('basement','地下室相对所属房屋向内缩进的距离，像素|地下室四周墙厚，像素|地下室楼梯相对房间左上角的偏移|地下室四个钩子相对房间左上角的坐标，必须四项|地下室刷新在白房的概率，0～1，其余为双层房');
group('pathfinding','寻路网格边长，像素|地图左右额外寻路空间，像素|单次寻路最多展开的网格数|直线通行检查的采样间距，像素');
group('ai','路径缓存基础秒数|路径缓存额外随机秒数|视为走到路径节点的距离，像素|AI 触发楼梯的距离，像素|AI 尝试翻窗的距离，像素|AI 翻窗时允许的纵向偏差，像素|AI 检测附近板子/尝试放板的距离，像素|愿意停下接受治疗的安全距离，像素|看到屠夫时开始逃跑的距离，像素|AI 放板时屠夫离板子足够近的距离，像素|接近当前逃跑目标到此距离时重选目标，像素|逃跑时搜索板区的范围，像素|选择板子另一侧逃跑点的偏移，像素|距离屠夫小于此值时考虑上楼避险，像素|上楼避险时搜索楼梯的范围，像素|逃跑方向采样数量，整数|逃跑候选目标的投射距离，像素|筛选逃跑落点的碰撞半径，像素|被地形遮挡的逃跑目标获得的评分奖励|AI 跳楼的接近距离，像素|AI 自动进入地窖的距离，像素|受伤 AI 被选为救援者时的评分惩罚|正在修机的 AI 被选为救援者时的评分惩罚|屠夫远离挂钩目标时认为适合救援的距离，像素|被挂超过此秒数后不再等待安全距离|AI 开始救援读条的距离，像素|倒地目标的治疗优先级奖励|搜索倒地队友的治疗范围，像素|搜索受伤队友的治疗范围，像素|AI 开始治疗读条的距离，像素|选择已打开出口的评分奖励|AI 开始开门读条的距离，像素|发电机靠近屠夫时判为危险的距离，像素|选择危险发电机的评分惩罚|每名正在修机的队友带来的选机评分惩罚|已修进度对选机评分的奖励|AI 开始维修读条的距离，像素|AI 开始挂人读条的距离，像素|屠夫选择受伤目标的评分奖励|屠夫把倒地者判为近距离目标的距离，像素|屠夫对近距离倒地者的优先级奖励|屠夫对较远倒地者的评分惩罚|屠夫按目标速度预测未来位置的提前秒数|屠夫记住最后目击位置的秒数|AI 抱起倒地者的距离，像素|追逐被倒板阻挡时搜索可破坏板子的范围，像素|AI 开始踩板的距离，像素|检查衣柜是否接近最后记忆位置的范围，像素|再次搜索同一个衣柜的最短间隔，秒|屠夫调查划痕/血迹的距离，像素|屠夫记住声音位置的秒数|到达最后记忆位置的判定距离，像素|AI 关闭地窖的距离，像素|屠夫巡逻优先选择有修理进度的发电机的评分奖励|同一个发电机算刚巡逻过的时间窗口，秒|刚巡逻过的发电机评分惩罚|已修好发电机的巡逻评分惩罚|AI 到达巡逻点/尝试踢机的距离，像素|目标附近的角色不再加入绕路障碍的距离，像素|目标移动超过此距离后重新寻路，像素|单次移动不足此距离视为卡住并重新寻路，像素|寻路终点可接受的误差，像素|逃跑目标坐标限制|没有可用逃跑候选点时的备用坐标');
group('touch','摇杆有效半径占控件宽度的比例|移动摇杆死区，屏幕像素|推杆距离占有效半径达到此比例后奔跑|拖动攻击键开始改变朝向的最小距离，屏幕像素');
group('physics','出口门碰撞宽度的一半，像素|出口门碰撞高度的一半，像素|屠夫出口屏障判定半高度，像素|屏障重复提示的最短间隔，秒|放倒木板长边的一半，像素|放倒木板短边的一半，像素|窗户阻挡碰撞的半宽，像素|窗户阻挡碰撞的半高，像素|被搬运者相对屠夫向上的坐标偏移，像素|被挂者相对钩子中心向下的坐标偏移，像素|挂钩姿势相对角色中心横向偏移，像素|挂钩姿势相对角色中心纵向偏移，像素；负值向上');
group('hud','手机消息提示距离屏幕顶部的像素数|手机方向/队友提示距离顶部的像素数|手机修机进度框距离顶部的像素数');
group('killerVisual','屠夫初版造型的体型倍率，1 为原大小；以脚底为锚点放大，攻击范围与碰撞仍由 core 和 movement 配置');
group('darkwoodVision','视野外环境压暗程度，0～1|视野轮廓基础射线数量；地形角点会补充射线|视野遮罩最短刷新间隔，秒；默认 0 随每帧显示位置更新，避免移动时阶梯跳动，静止时复用|大石头遮挡视线的最小短边尺寸，世界像素；短边小于此值的小石头不遮挡，碰撞不变');
group('network','本机移动预测最多保留的未确认秒数|服务器位置纠偏的平滑时间常数，秒|纠偏超过此距离直接对齐服务器，像素|网络断流时停止本机预测的秒数|每 N 个 UDP 快照发送一次可靠关键帧|远端快照断续时最多外推秒数，超过后停在末端|房主允许客户端移动积攒的最大时间预算，秒|首次客户端移动上报的初始时间预算，秒');
group('rendering','画布最低采样倍率，1.5 表示每个 CSS 像素使用 1.5 个画布像素|高分屏画布采样倍率上限，控制显存与填充开销|手机画布采样倍率，1 表示按屏幕 CSS 像素渲染，降低 GPU 开销');
Object.assign(descriptions,{
 'camera.survivor.fov':'求生者镜头视角，度；变大会缩小画面物件、看得更广','camera.killer.fov':'屠夫镜头视角，度；变大会缩小画面物件、看得更广','camera.survivor.z':'求生者虚拟镜头高度，越大视野越广','camera.killer.z':'屠夫虚拟镜头高度，越大视野越广',
 'camera.fit.mobileMin':'手机自适应缩放的最小值','camera.fit.mobileMax':'手机自适应缩放的最大值','camera.fit.mobileWidth':'手机缩放基准宽度，屏幕像素','camera.fit.mobileHeight':'手机缩放基准高度，屏幕像素','camera.fit.desktopMin':'桌面自适应缩放的最小值','camera.fit.desktopMax':'桌面自适应缩放的最大值','camera.fit.desktopWidth':'桌面缩放基准宽度，屏幕像素','camera.fit.desktopHeight':'桌面缩放基准高度，屏幕像素',
 'audio.heartbeat.interval':'无危险时的心跳间隔基值，秒','audio.heartbeat.intervalDrop':'满危险时心跳间隔减少的秒数','audio.heartbeat.gain':'开始有危险时的心跳音量基值','audio.heartbeat.gainRise':'满危险时心跳音量额外增量','audio.heartbeat.exponent':'心跳音量随危险值变化的曲线指数','audio.heartbeat.cutoff':'心跳低通滤波基础截止频率，Hz','audio.heartbeat.cutoffRise':'满危险时截止频率额外增加值，Hz',
 'world.size':'地图总尺寸；修改时需同步边界墙、出口和寻路范围','world.upperFloor':'二楼有效矩形，同时用于二楼碰撞边界','world.buildings':'两栋房屋的区域矩形，影响地板和地下室定位','world.stairs':'地面和二楼的连接楼梯','world.drops':'二楼向地面的单向跳落连接','world.walls':'基础墙、树、杂物和石块碰撞矩形数组','world.rocks':'不规则石头的矩形；画面与分段碰撞同步使用，四周需保留通路','world.genSpots':'发电机候选点，按楼层分别随机抽取','world.hookSpots':'地上钩子候选点，随机抽取 outdoorHooks 个','world.palletSpots':'固定板子坐标，必须配套两侧地形','world.windowSpots':'固定窗户坐标，应位于墙体缺口','world.lockers':'衣柜中心坐标，柜门交互点另有向下偏移','world.survivorStarts':'四名求生者的出生候选位置，随机分配','world.killerStart':'屠夫出生位置','world.gates':'两个出口门的中心坐标，数组顺序为左、右','world.hatch':'逃生地窖的坐标，与地下室不是同一物件','world.interiorWalls':'新增室内隔墙、家具碰撞矩形数组','world.interiorWindows':'新增室内窗坐标','world.interiorPallets':'新增室内板子坐标'
});
const leafNames={x:'横向坐标或相对横向偏移，像素',y:'纵向坐标或相对纵向偏移，像素',w:'矩形宽度，像素',h:'矩形高度，像素',right:'允许的最大横坐标，像素',bottom:'允许的最大纵坐标，像素',left:'允许的最小横坐标，像素',top:'允许的最小纵坐标，像素',level:'楼层：0=地面/一楼，1=二楼，-1=地下室；省略视为 0',kind:'碰撞地形类型：wall=墙、tree=树、junk=杂物、rock=石头',low:'true 表示低矮地形，不遮挡视线，保留碰撞',axis:'板子 axis=x 表示沿 x 方向穿过板子、板子长边沿 y；y/省略表示沿 y 穿过。窗户当前使用 x 向翻越',name:'房屋显示名称',pad:'矩形四周额外禁止生成板区的留白，像素'};
function describe(path){if(descriptions[path])return descriptions[path];const key=path.split('.').at(-1);if(leafNames[key])return leafNames[key];throw Error('Missing field description: '+path);}
function flatten(value,path,rows){
 if(Array.isArray(value)&&value.some(v=>v&&typeof v==='object')){rows.push([path,'数组，'+value.length+' 项',describe(path)]);const merged=Object.assign({},...value);for(const [k,v] of Object.entries(merged))flatten(v,path+'[].'+k,rows);}
 else if(value&&typeof value==='object'&&!Array.isArray(value)){if(descriptions[path])rows.push([path,'对象',descriptions[path]]);for(const [k,v] of Object.entries(value))flatten(v,path+'.'+k,rows);}
 else rows.push([path,JSON.stringify(value),describe(path)]);
}
let md='# game-config.json 逐字段说明\n\n配置入口：[src/game-config.json](src/game-config.json)。修改流程见 [CONFIGURATION.md](CONFIGURATION.md)。本表由 `node scripts/config-fields.mjs` 生成，列出的当前值以生成时的 JSON 为准。\n\n时间=秒，距离=世界像素，速度=像素/秒；明确标注屏幕像素的字段用于界面。概率/进度使用 0～1。`[]` 表示数组中每一项，坐标数组行的值是示例，不是所有点都相同。JSON 中 `_说明` 与 `_字段说明` 为阅读说明，不参与游戏规则。\n\n搬运挣脱：每秒进度为 `1 / carryPlayerTime`，每次有效主动挣扎再加 `wiggleGain`。输入间隔不能短于 `wiggleInterval`。当前玩家自然挣脱 32 秒，连续有效挣扎理论约 `1 / (1/32 + 0.012/0.25) = 12.6` 秒，实际受帧间隔影响。AI 自然挣脱 22 秒。\n';
let total=0;for(const [name,value] of Object.entries(config)){if(name.startsWith('_'))continue;const rows=[];flatten(value,name,rows);total+=rows.length;md+='\n## '+name+'\n\n| 字段 | 当前值 / 类型 | 含义与单位 |\n| --- | --- | --- |\n';for(const [path,v,description] of rows)md+='| `'+path+'` | `'+v+'` | '+description+' |\n';}
md=md.replace(/当前玩家自然挣脱[\s\S]*?AI 自然挣脱 22 秒。/,`当前玩家自然挣脱 ${config.core.carryPlayerTime} 秒，连续有效挣扎理论约 ${(1/(1/config.core.carryPlayerTime+config.core.wiggleGain/config.core.wiggleInterval)).toFixed(1)} 秒，实际受帧间隔影响。AI 自然挣脱 ${config.core.carryAITime} 秒。`);
fs.writeFileSync('CONFIG-FIELDS.md',md);console.log({documentedFields:total});


